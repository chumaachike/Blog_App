<% @users.each do |user|%>
  <div class="user">
    <img src= <%=user.photo%> alt="user photo">
    <div class="image">
      <h5><%= link_to user.name, user%></h5>
      <p>Number of posts: <%= user.post_counter%></p>
    </div>
  </div>
<% end %>